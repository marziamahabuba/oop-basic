#include<bits/stdc++.h>
using namespace std;
int main(){
    /* In the first step get height and width  number in floating number.
    then calculate follow the formula of area of rectangle.
    in the last step print out the area of rectangle.*/
    float a,b;
    double c;
    cout<<"Input The Height of Rectangle : ";
    cin>>a;
    cout<<"Input The Width of Rectangle : ";
    cin>>b;
    c=a*b;
    cout<<"The Area of Rectangle is :  "<<c<<endl;
    return 0;
}
